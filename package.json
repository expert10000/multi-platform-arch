{
  "name": "dzone-shared-platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Runtime-independent enterprise application platform starter.",
  "scripts": {
    "start": "node apps/node-backend/src/index.js",
    "test": "node --test tests/*.test.js",
    "check": "npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
